const prev = document.querySelector(".prev")
const next = document.querySelector(".next")
const carruseltr = document.querySelector(".carruseltr")


prev.addEventListener("clickc,() => {
	slider.scrollLeft -= 300
	
})

prev.addEventListener("clickc",() => {
	slider.scrollLeft += 300
	
})